---
name: sitio-publico
description: Usar para tareas sobre las páginas públicas del sitio (home, nosotros, productos, blog, FAQ, contacto), SEO (sitemap, robots, 404) y la capa de caché/revalidación del contenido público.
---

Eres el agente del **sitio público** de cmc-website (Next.js 15 App Router, SSG, todo en español).

## Archivos clave

- Rutas públicas: `src/app/(public)/` (home, `nosotros`, `productos` + `[slug]`, `blog` + `[slug]`, `preguntas-frecuentes`, `contacto`; chrome compartido en `(public)/layout.tsx`). El footer incluye un enlace discreto **Admin** → `/admin` (pedido de la clienta, 2026-08-19).
- Fundación de diseño: `PRODUCT.md` y `DESIGN.md` (+ sidecar `.impeccable/design.json`) en la raíz — North Star "El Obrador Editorial" y reglas nombradas (Amber Guardrail, One Hero, Eyebrow, Flat-At-Rest). Consultarlos antes de cualquier cambio visual; son vinculantes hasta que llegue el manual de identidad.
- Componentes compartidos: `src/components/public/{shared,ProductDetail,ProductGallery,PostArticle,MobileNav,BrandsMarquee}.tsx` — `shared.tsx` (`ProductCard`, `PostCard`, `EditorialCover`, `SectionHeading` con props opcionales `id`/`size`/`tone`, `tone="warm"` por defecto: eyebrow naranja + título petróleo; `tone="onDark"` (2026-08-28) para las bandas de color plenas: eyebrow ámbar + título blanco + descripción `white/80`) lo usan también `/productos`, `/blog` y las vistas previas del admin: cambios solo aditivos. `ProductCard` y `ProductDetail` renderizan el empaque sobre lienzo blanco con `object-contain` — **nunca** `object-cover` sobre packshots. `ProductDetail` acepta `settings` opcional: con WhatsApp configurado el CTA abre `wa.me` con el nombre del producto prellenado. `ProductDetailData` incluye `technicalSheet` (ficha técnica PDF): si existe, `ProductDetail` muestra el CTA secundario outline «Descargar ficha técnica (PDF)» junto al CTA principal (`<a download>` a la ruta estable del medio; nunca URLs internas de R2); sin ficha, el botón no se renderiza. La visibilidad de productos sigue dependiendo solo de `DRAFT`/`PUBLISHED` vía RLS — sin filtros por nombre/slug en componentes. **`ProductGallery` (2026-08-19)** es el segundo client component público (junto a `MobileNav`): recibe `gallery: MediaAsset[]` + `productName` desde `ProductDetail` (que sigue siendo Server Component); miniaturas como botones con `aria-pressed` (nunca enlaces al archivo crudo), flechas/Home/End, táctil ≥44 px, imágenes apiladas en una celda de grid (cross-fade `--dur-fast`, sin layout shift) y visor `<dialog>` nativo (estilos `.product-lightbox` en `globals.css`; controles petrol-deep translúcido para contrastar sobre lienzo blanco). No reintroducir `<a href>` a los `.webp`.
- Piezas de identidad y color (2026-08-28): `src/components/public/{BrandLockup,BrandStripe,ClimateVariants,RollingPinOrnament}.tsx`. **`BrandLockup`** (header y pie) monta `logo-cmc-emblema.svg` —solo el símbolo, `viewBox` recortado a la tinta de los dos trazos— más la razón social y el lema **como texto en rojo** (`text-accent`, 5.08:1 sobre blanco), con el lema centrado bajo el nombre (`w-fit` + `text-center`); en móvil el texto no se oculta pero lleva techo de ancho (`max-w-[11rem]`) para envolver en vez de empujar al menú. Regla: **el texto del logotipo se compone como texto**, el lockup dibujado solo donde se pinta grande (hero). **`BrandStripe`** es decorativa (`aria-hidden`), proporciones 5/3/2 azul-verde-rojo. **`ClimateVariants`** vive en la home y al cierre de `/productos`; su copy es constante del componente y **todas las cifras salen literales de las fichas técnicas** (registrado en `docs/CONTENT_PENDING.md` §2b). **`RollingPinOrnament`** es SVG de trazo con `currentColor`, no un GIF.
- Menú «Productos» con desplegable (`DesktopNav`/`MobileNav`, 2026-08-28): «Productos» sigue siendo enlace y suma un botón chevron con `aria-expanded`/`aria-controls`; abre por hover y por clic, cierra con Escape y al salir el foco del grupo (`onBlur` comprobando `relatedTarget`). Los destinos salen de `productMenuItems()`, fuente única para escritorio y móvil. Con `CATALOG_PDF_HREF === null` (`src/lib/catalog.ts`) la entrada «Descargar catálogo» se pinta inerte con la etiqueta «Próximamente»; al apuntar la constante a un PDF se vuelve enlace real, sin tocar componentes.
- Componentes exclusivos de la home: `src/components/public/{HomeHero,HomeStats,HomePillars,HomeProductCard,HomePostsSection,HomeCta}.tsx`. `HomeHero` recibe `hero` (company_content) y `settings`. **Rediseño 2026-08-28 (requerimiento 06), recompuesto el 2026-08-30 tras revisión de la clienta**: la composición derecha es un **escenario que se turna** entre las 3 fotos de aplicación de los destacados —caja + horneado, `STAGE_PHOTOS`, rutas literales— sobre un **disco azul lavado** (`bg-tint-blue`), con el logotipo animado como **sello de 160–176 px arriba a la DERECHA**. Todo con `mix-blend-multiply` (fotografiadas sobre blanco puro). La versión del 2026-08-28 apilaba los tres **empaques** girados y la clienta la descartó («no me convencen las cajas… que se vieran panes o torta, que destaque el resultado»), y pidió el logo «al otro lado y un poquito más grandecito». **Regla que deja el episodio: en el hero el empaque nunca va solo.** Motion en `.hero-stage` (globals.css): ciclo de 18s, fundido cruzado de 0.72s, retardos NEGATIVOS (0, -12s, -6s) para que las tres animaciones corran desde el frame 1 y la primera —candidata a LCP— nazca visible; pausa en hover y focus-within. El eyebrow **ya no lee `settings.slogan`** (el lema vive ahora en el lockup del header y se duplicaba en el mismo viewport): es un descriptor fijo. El sello conserva el **relevo GIF → vector** del **2026-08-23**: `cmc-logo-entrada-una-vez.gif` cubre la animación y a los 2 s cede el puesto a `/brand/logo-cmc.svg` (el GIF solo tiene 512 px y allí se pinta a ~640 px, así que su frame congelado se veía interpolado). Es un relevo **seco y simultáneo** (`logo-relevo-sale`/`logo-relevo-entra` con `step-end` en `globals.css`), nunca un crossfade: las dos capas jamás se ven superpuestas. El `viewBox` del SVG está recortado a la misma tinta que el frame final, y ambas capas viven en un contenedor `aspect-[512/340]` con `mix-blend-multiply` **en el contenedor, no en cada imagen** (si no, el solape multiplicaría dos veces contra el crema). Con reduced-motion el GIF no se monta y queda solo el vector — decisión de la clienta (2026-08-19) que reemplazó el packshot del primer producto publicado y el círculo ámbar original. **Fondo rotativo del hero (2026-08-20, pedido de la clienta)**: capa `aria-hidden` con `-z-10` dentro de la sección `isolate` (`.hero-slides` en `globals.css`), `HERO_SLIDES` en crossfade CSS puro a opacidad textura (`--hero-slides-opacity` 0.12) detrás de todo el contenido — desde el 2026-08-30 son **3 escenas de producto terminado, sin una sola caja**: al ampliarse a pantalla completa, una foto de aplicación dejaba un empaque gigante y legible detrás del titular. El ciclo bajó a 18s para compartir compás con el escenario, y **los porcentajes de `hero-slide-cycle` están atados a esa duración**: si cambia el número de fotos hay que recalcularlos (entrada 1.5s, turno 6s, salida 7.5s ÷ duración total) — texto, círculo y logos intactos encima; aquí `object-cover` SÍ es válido porque las fotos son textura decorativa, no packshots-contenido; con reduced-motion queda solo la primera foto estática. `HomeStats` calcula indicadores del catálogo y se oculta sin datos. `HomeProductCard` (en la home **3 destacados en una fila** + «Ver catálogo», `products.slice(0, 3)` — 2026-08-28, eran 2 desde el 2026-08-20; son los tres del orden destacado de `sortProductsByFeatured`) renderiza la imagen sobre lienzo blanco uniforme con `object-contain`, sin recortes ni filtros — admite archivos con cualquier fondo, proporción u orientación subidos desde el admin; no reintroducir fondos por índice ni `object-cover`. `HomePostsSection` genera portadas editoriales CSS para posts sin cover y omite fechas nulas/ inválidas; desde el **2026-08-21** el archivo exporta además **`HomePostsRotator`**, que es la cabecera del blog **en la home y en `/blog`** (`HomePostsSection` queda solo de fallback cuando hay un único artículo). El rotador reparte los mismos artículos entre un escenario que se turna con fundido **encadenado** —5.4 s en pantalla, 0.6 s de salida, 0.6 s de entrada, **sin solaparse**: cruzar dos titulares a la vez se lee como visión doble— y un índice lateral cuya barra ámbar marca el que está en escena; ambos comparten reloj vía `animation-delay` por `nth-child` (`.blog-rotator` en `globals.css`, ver DESIGN.md). Ojo al tocar ese CSS: usar propiedades largas (`animation-name`/`-duration`), nunca el atajo `animation`, que resetea `animation-delay` y hace arrancar todos los turnos a la vez. Piezas compartidas internas `PostStage`/`PostRow` — al tocar una, se tocan las dos vistas. Las dos páginas ordenan con **`sortPostsByCoverFirst`** (`src/lib/content.ts`): primero los artículos **con portada**, para no encabezar una superficie fotográfica con un bloque tipográfico mientras hay fotos en las miniaturas; los que no tienen foto caen a las tarjetas del final de `/blog`. Se corrige cargando la portada que falte, no cambiando ese orden. **Covers fotográficos (2026-08-21)**: se suben **desde el panel** (`/admin/blog/<id>` → «Imagen de portada»), así que son medios R2 normales del CMS; `public/images/blog/` solo guarda los derivados optimizados que la usuaria elige al subirlos y se borrará —con el script de respaldo `2026-08-20-covers-blog.sql`— cuando estén cargadas. Verificado el 2026-08-21: los 4 artículos siguen con `cover_image_id` en null (portadas tipográficas) porque la subida desde el panel estaba rota por ids duplicados en `UploadImageForm` — corregido el mismo día, ver `panel-admin`. El artículo de almacenamiento de materias primas se queda sin foto a propósito. `HomeCta` solo muestra canales configurados en `site_settings`. **Fotos editoriales (2026-08-19, reparto final tras iteraciones de la clienta)**: la home muestra los **recortes con transparencia** en dos sitios — «Propuesta de valor» llevaba SOLO las palmeritas hasta el **2026-08-20**, cuando la clienta las reemplazó por la **escena real** `panadero-croissants-01.webp` (panadero con carro de croissants; figura única `w-full` enmarcada — `overflow-hidden rounded-lg border border-border` — en la columna derecha del grid `lg:grid-cols-[3fr_2fr] lg:items-center` sobre crema profunda). Esa foto **no viene de `content-source/`**: origen sin documentar y aspecto de stock, contra la regla «cero fotografía de stock» (ver `docs/FOTOS_ADICIONALES.md`). Antes de las palmeritas, la composición de 3 había salido del hero, que quedó solo con el logo y en «¿Quiénes somos?» los buñuelos **salieron el 2026-08-20**: la clienta los reemplazó por la **escena real de bodega** `bodega-dap-01.webp` (1200×845, derivada de una de las fotos que `docs/FOTOS_ADICIONALES.md` había descartado por indicios de IA — elección explícita suya, origen sin confirmar). Al ser escena real y no packshot **no se recorta y conserva marco**: `figure` con `overflow-hidden rounded-lg border border-border` y la imagen a `w-full` (DESIGN.md, «Recorte editorial flotante»). El grid de esa sección lleva `lg:items-center` para que el texto quede centrado verticalmente contra la columna de la foto (pedido de la clienta, mismo día). En las figuras con `max-w` (palmeritas, /nosotros) los props `width`/`height` del `<Image>` deben ser SIEMPRE las dimensiones reales del derivado — reservan espacio, no fijan el tamaño visible, que lo gobierna `max-w`. `/nosotros` (figura de cierre) igual en proporción natural; bajo el h1 de `/nosotros` va, desde el **2026-08-23**, la escena `photos/cargue-cajas-dap-01.webp` (cargue de cajas DAP en el camión; enmarcada `overflow-hidden rounded-lg border border-border` y con alt descriptivo por ser escena real, no decoración). Reemplazó a la **ilustración de panes dibujada a mano** `decorative/quienes-somos-panes.webp` (2026-08-19), que a su vez había reemplazado a `hero-mesa-panaderia-01`; ambas quedan disponibles sin uso. ⚠️ La foto nueva **repite los indicios de IA** de la alerta de bodega/transporte y muy visiblemente: cajas que dicen «AP», pie «Compañía Mundial de **Cereales** S.A.S.» (la empresa es *de Comercio*) y texto menor corrupto — publicada por pedido explícito de la clienta, pendiente de confirmar origen (`docs/FOTOS_ADICIONALES.md`). La canasta vive solo en la página FAQ; el recorte del surtido de amasijos queda disponible sin uso, y los lienzos completos de la mesa y del surtido pasaron a la textura del fondo del hero (2026-08-20). **Ninguna foto se repite en el sitio** — con la excepción documentada (2026-08-20, `docs/FOTOS_ADICIONALES.md`) de las 5 fotos de aplicación de producto que reaparecen en el fondo rotativo del hero solo como textura semi-transparente. Derivados `-recorte.webp` generados por `scripts/recortar-fotos-editoriales.mjs`; rutas literales, sin `media_assets`. El lienzo blanco + `object-contain` queda EXCLUSIVO de packshots de producto. No repetir la misma foto en varias secciones ni usar las fotos de bodega descartadas, con la **única excepción autorizada por la clienta** (2026-08-20): `bodega-dap-01.webp` en «¿Quiénes somos?» (`docs/FOTOS_ADICIONALES.md`). El recorte de buñuelos queda disponible sin uso. El círculo blanco del hero va centrado con el emblema del logo (`left-1/2 top-1/2` a secas): desde el relevo al vector (2026-08-23) la geometría es idéntica con y sin reduced-motion, así que se retiró el override `motion-reduce:top-[40%]` que compensaba el encuadre distinto del PNG estático anterior.
- Ornamentos de obrador por sección: `src/components/public/BakerySideOrnament.tsx` (server component con prop `side: "izquierdo" | "derecho"`) — dibujo botánico a mano en ambos márgenes (`/images/decorative/borde-ornamental-cmc.png` y `borde-ornamental-cmc-derecha.png`, espejo exacto con lienzo 887×1774 y la misma constante 0.158; rutas literales). **Desde el 2026-08-20 (pedido de la clienta) ya NO son fixed ni globales**: son `position: absolute` dentro de una sección anfitriona (scrollean con ella) y se montan SOLO en **dos** anclas desde el 2026-08-28 — zona alta de /nosotros (título + ilustración) y la página de contacto. La tercera, los pilares de la home, la retiró la clienta junto con el fondo crema; allí la decoración es ahora `RollingPinOrnament` (prop `withOrnament` de `HomePillars`). Requisito del host: full-bleed (el clamp usa 100vw), `relative` y `overflow-x-clip` (al no ser fixed, el recorte es lo que evita el scroll horizontal). Decorativos puros: `alt=""` + `aria-hidden`, `pointer-events: none`, centrados en su sección, opacidad 0.7, ocultos < lg. Geometría en `.bakery-side-ornament` + variante `--derecha` de `globals.css` (altura `min(86%, 900px)` relativa al host, ancho auto, `z-index: 1`; `left`/`right` deslizan el dibujo fuera del lienzo en viewports angostos para que la tinta nunca invada la columna de texto). No volver a montarlos globales ni convertirlos en patrón repetido, ni añadirles fondos/sombras/animación; si el cliente entrega otra versión del dibujo, re-medir el bounding box de la tinta y ajustar la constante 0.158.
- Acordeón de FAQ único del sitio: `src/components/public/FaqAccordion.tsx` (2026-08-19, server component) — acordeón editorial con numeración naranja, chevron en círculo mostaza, encabezado abierto en petróleo con texto blanco, respuesta sobre lino (`surface-muted` desde el 2026-08-28) y **una sola pregunta abierta** vía `<details name>` nativo (sin JS; degrada a múltiples abiertas). Lo usan `/preguntas-frecuentes` y la sección de destacadas de la home; el `FaqList` compacto de `shared.tsx` se **eliminó** el 2026-08-19 al unificar (pedido de la clienta: los dos acordeones se veían distintos). La página FAQ monta un panel `petrol-deep` con eyebrow ámbar (el naranja no contrasta sobre petrol-deep), círculo mostaza y el recorte con transparencia `canasta-panes-surtidos-01-recorte.webp` (generado por `scripts/recortar-fotos-editoriales.mjs`, registrado en el manifest); el sticky del panel solo aplica con altura de viewport ≥ 53rem.
- Componentes exclusivos de `/contacto` (2026-08-20, datos oficiales entregados por la clienta): `src/components/public/{ContactMap,AudienceSectors}.tsx`. **`ContactMap`** embebe Google Maps en un `<iframe>` (`?q=…&output=embed`) **sin API key y sin campo nuevo en la BD**: la consulta se deriva de `site_settings.address` + `.city`, así que editar la dirección desde el admin mueve el mapa. Exporta también `mapsSearchHref()`, que usa la fila «Dirección» de la lista de canales para que enlace y mapa apunten al mismo sitio. Antes de consultar, `buildMapQuery` **descarta los segmentos de detalle interior** (`Of.`, `Oficina`, `Torre`, `Piso`, `Local`, `Apto`, `Bodega`) porque Google no los geocodifica y engancha la ficha de una empresa vecina (verificado 2026-08-20: con «Torre Ofiespacios, Of. 325-326» el pin salía rotulado «Office To Go S.A.S»); la dirección completa se sigue mostrando como texto en el `figcaption` y en la lista. Devuelve `null` sin `address`, como el resto de la página. **`AudienceSectors`** es la banda de cierre `bg-surface-muted` (era `bg-cream` hasta el 2026-08-28) con los 12 sectores a los que vende la empresa: lista con divisores en 3 columnas y marcador ámbar decorativo (forma, no texto — Amber Guardrail), `reveal` por ítem, **sin numeración** (no hay jerarquía entre sectores). Va FUERA del wrapper de ornamentos, como sección hermana full-bleed, para que los `BakerySideOrnament` no la invadan y se conserve el ritmo de fondos alternos. Su copy es **contenido fijo en el componente**: todavía no hay sección de `company_content` para editarlo (registrado en `docs/CONTENT_PENDING.md`).
- Iconos: `src/components/public/icons.tsx` (2026-08-20) — `WhatsAppIcon` y `PhoneIcon`, SVG inline con `currentColor` + `aria-hidden` (no hay librería de iconos y no se debe añadir). Son los **únicos iconos rellenos** del sitio; el resto del vocabulario es de trazo 2px (chevron de FAQ, menú móvil, controles de galería) y así debe seguir. Se montan en los tres CTA de contacto: `/contacto` (WhatsApp + llamada, `size-5`), `HomeCta` (`size-5`, hereda petróleo profundo sobre el ámbar) y `ProductDetail` (`size-4`). El glifo de WhatsApp es marca registrada: no alterar el trazado ni recolorearlo. El `viewBox` de `PhoneIcon` es `2 2 20 20` a propósito — su trazado ocupa 18×18 y en un lienzo de 24 se veía un 25 % más pequeño que el de WhatsApp; si se cambia el trazado, re-medir con `getBBox()`. En `/contacto` el botón outline lleva `py-3` (no `py-3.5`) para compensar su borde de 2 px y que ambos midan 52 px apilados en móvil — misma convención que el CTA outline de `ProductDetail` (`py-[10px]`).
- Fetchers de contenido: `src/lib/content.ts` — incluye `sortProductsByFeatured`/`FEATURED_PRODUCT_SLUGS` (2026-08-28): `getPublishedProducts` sube al frente Alta Repostería Ponqué, Repostería y Hojaldre (los más vendidos según la clienta) y deja el resto con su `sort_order`. Al aplicarse en el fetcher, home y `/productos` no pueden divergir.
- Revalidación: `src/lib/revalidate.ts` (`CACHE_TAGS`: `settings`, `content`, `products`, `posts`, `faqs`, `brands`; `revalidatePublicContent()`)
- Navegación y etiquetas: `src/lib/nav.ts`, `src/lib/section-labels.ts`, `src/lib/catalog.ts` (catálogo PDF)
- SEO: `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/not-found.tsx`
- Markdown: `src/lib/markdown.tsx`

## Reglas de arquitectura (no negociables)

- Las páginas públicas **nunca** consultan Supabase por visita. Todo dato pasa por los fetchers de `src/lib/content.ts`, envueltos en `unstable_cache` con tags de `CACHE_TAGS` y **sin revalidación por tiempo** — solo revalidación bajo demanda desde el admin.
- Los fetchers lanzan error en vez de cachear fallos (resiliencia ante el auto-pause de Supabase free-tier); las rutas no generadas muestran el componente `DataUnavailable`.
- Markdown siempre se renderiza sanitizado vía `src/lib/markdown.tsx` (react-markdown + rehype-sanitize, sin HTML crudo). Desde 2026-08-21 el cuerpo de los artículos puede traer imágenes (`![alt](/api/media/<key>)`, subidas desde el panel): el esquema por defecto de `rehype-sanitize` **sí** admite `src` relativa y conserva el `alt` (verificado), y el marco lo pone `.prose-cmc img` en `globals.css`. No son `next/image` (el Markdown no lleva dimensiones y el optimizador está apagado), así que `markdown.tsx` mapea `img` para añadir `loading="lazy"`/`decoding="async"` a mano; van sin `width`/`height` y ese CLS se asume a cambio de que la proporción nativa mande.
- Solo se muestra contenido con estado `PUBLISHED`.
- `next/image` corre con `unoptimized: true`; no reintroducir el optimizador de Next.
- Respetar las restricciones al final de `docs/ARCHITECTURE.md`.

## Tema visual público

- Tokens en `src/app/globals.css` (Tailwind v4, `@theme inline`, sin tailwind.config). Paleta cálida del rediseño: `petrol`/`petrol-deep` (titulares e institucional), `amber` (decorativo/botón sobre petróleo; **nunca** texto sobre fondos claros), `orange` (eyebrows/numeración, AA sobre claro). **Giro de fondos 2026-08-28**: los `cream*` salieron del sitio público (la clienta los descartó) y el fondo pasó a blanco/lino con el color en **bandas plenas** —indicadores en `bg-secondary` azul, pilares en `bg-green-deep`, CTA en petróleo— y **franjas tricolores** (`BrandStripe`: 3 px bajo el header, 6 px como divisor de «Propuesta de valor» y remate del pie). Token nuevo: `tint-blue` (disco del hero). Los tokens `cream*` siguen definidos porque los usa el admin: no borrarlos.
- Fraunces (display) solo aplica dentro de `.public-site` (clase del layout público) sobre `h1–h3` y vía la utilidad `font-display`; el admin conserva Geist salvo su h1 por página, que desde el 2026-08-28 usa `font-display` explícitamente. No añadir reglas tipográficas globales.
- `mix-blend-multiply` del hero requiere packshots con fondo blanco y `isolate` en la sección.
- **Motion CSS-first** (vocabulario al final de `globals.css`): tokens `--ease-out`/`--ease-drawer`/`--dur-*`; entrada del hero `.enter`/`.enter-2..4`/`.enter-lcp`/`.enter-visual` (los candidatos LCP animan solo transform, nunca opacity); reveal scroll-driven `.reveal` (`animation-timeline: view()` bajo `@supports`, solo en la home y solo tarjetas/ítems, nunca prosa); acordeón FAQ vía `::details-content` + `interpolate-size` (scoped a `.public-site`); drawer móvil `.nav-drawer` y desplegable de productos `.nav-dropdown` con `@starting-style`; rodillo de la banda de pilares `.rodillo`/`.rodillo-vetas` (vaivén de 7 s con `ease-in-out`; los nudos usan la MISMA onda que el rodillo, así avanzan el doble en absoluto — es lo que hace la superficie de un cilindro que rueda); view transitions cross-document con `site-header` estable; fondo rotativo del hero `.hero-slides`/`.hero-slide` (`hero-slide-cycle`: ciclo 42s, 7 turnos de 6s con fundido de 1.5s y Ken Burns 1→1.06, delays por `nth-child`; solo opacity/transform, sin will-change; reduced-motion = primera foto estática); relevo del logo del hero `.hero-logo-gif`/`.hero-logo-vector` (`logo-relevo-sale`/`logo-relevo-entra`, 2s `step-end forwards`: los 2s salen del GIF, cuya animación termina a los 1.54s — suma de los 11 primeros retardos —, más ~460ms de margen para el desfase entre el reloj CSS y la decodificación del GIF; el `opacity: 0` inicial del vector vive DENTRO del media query de no-preference para que reduced-motion lo deje visible). Prohibido: librerías de animación JS, IntersectionObserver, convertir componentes públicos a client components por motion. Todo motion nuevo respeta `prefers-reduced-motion` (patrón CTA: `active:scale-[0.98] motion-reduce:active:scale-100`, sin matar las transiciones de color).
- **GIFs de marca**: **tres** momentos desde el 2026-08-28 (sello del hero, logo DAP junto al encabezado del catálogo de la home, mantequilla del CTA). El header perdió el suyo: monta `logo-cmc-emblema.svg` vía `BrandLockup`, más grande y nítido, sin animación — el logotipo se anima una sola vez por visita. No añadir más sin decisión explícita del cliente; footer y demás secciones siguen sin GIFs. En el hero el GIF **solo dura la animación**: al terminar entrega el puesto al vector (relevo 2026-08-23). El logo DAP no lo necesita porque allí el GIF se pinta más pequeño que su tamaño nativo (256 px) y no se interpola.

## Convenciones

- Código, comentarios, copy y commits en español; identificadores en inglés (convención existente).
- Rutas con segmentos en español (`/nosotros`, `/productos`, `/preguntas-frecuentes`).

## Verificación

No hay framework de tests JS. Verifica con `npm run lint` y `npm run typecheck`; para comprobar en el runtime real de Workers, `npm run preview`.

## Mantenimiento del contexto

Si tu cambio agrega/renombra rutas públicas, componentes de `src/components/public/`, tags de caché o fetchers, actualiza **en el mismo turno**: este archivo, `docs/ARCHITECTURE.md` y `src/app/sitemap.ts` (si hay ruta nueva). El cambio no está terminado si la documentación describe el estado anterior.
3